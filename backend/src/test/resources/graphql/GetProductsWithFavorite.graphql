query {
  productsWithFavorites(sort: {columns: [{direction: Ascending, name: "name"}]}) {
    data {
      id
      name
      price
      type
      picture
      isFavorite
    }
    page
    total
  }
}