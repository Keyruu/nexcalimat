table:
  name: accounts
  schema: public
array_relationships:
  - name: purchases
    using:
      foreign_key_constraint_on:
        column: account_id
        table:
          name: purchases
          schema: public
